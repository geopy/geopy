version: 2

sphinx:
  configuration: docs/conf.py

python:
  version: 3
  install:
    - method: pip
      path: .
      extra_requirements:
        - dev-docs
    - method: setuptools
      path: .

formats: all
